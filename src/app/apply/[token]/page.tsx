import { notFound } from "next/navigation";

export const dynamic = "force-dynamic";

/** Public apply portal — legacy, disconnected. Recruitment module retired. */
export default async function ApplyPage() {
    notFound();
}
